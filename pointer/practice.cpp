#include <iostream>
using namespace std;
int main()
{
    // int a[] = {4, 5, 6, 7};
    // int *c = &a;
    // c = c + 1;
    // cout << *(a) << " " << *(a + 1) << endl;
    // cout << *(a + 2);
    // cout << *a + 9;

    char s[] = "hello";
    char *p = s;
    cout << s[0] << " " << p[0];
}